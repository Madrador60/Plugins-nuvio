# Controllers

Les routes principales sont encore servies par `src/server/app.js` pour garder la compatibilite avec le serveur existant.
Les prochains controllers doivent etre extraits ici progressivement, route par route, sans casser Render.

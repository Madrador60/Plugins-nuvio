# Archive

Ce dossier garde les anciens fichiers qui ne sont plus utilises directement par la V2.

## old/

- `site-package.json` : ancien `package.json` local au dossier `site/`. La configuration officielle est maintenant le `package.json` racine.
- `site-README.md` : ancienne note locale du serveur. La documentation officielle est maintenant le `README.md` racine et les fichiers dans `docs/`.

Les fichiers sont archives au lieu d'etre supprimes pour garder une trace et eviter de casser une reference externe inattendue.
